# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
# dist/ is intentionally committed — the droplet has no Node and
# serves the dashboard via Nginx straight from this directory after a
# `git reset --hard origin/main`. See the root .gitignore for the
# matching policy comment.
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
